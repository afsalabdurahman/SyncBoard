// import { inject } from "tsyringe";
// import { IActivity } from "../../application/repositories/IActivity";
// class Logger   {
//     constructor( @inject("ActivityUsecase") private _activityUsecase: IActivity){};

//      createActivity(name:string,createdBy:string,activityId:string){
//         this._activityUsecase.projctActivity(name,createdBy,activityId)
//     }

// }