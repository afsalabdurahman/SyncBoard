// // import { Types,ObjectId } from "mongoose";
// // import { CreateActivityDTO } from "../../application/dto/LogDTOs";
// // import {IActivityRepository} from "../../domain/interfaces/repositories/ILogRepository"
// // import { IActivity } from "../database/models/LogModel";
// // import {ActivityModel} from "../database/models/LogModel"
// // import {ILog} from "../../application/repositories/ILog"

// export class LogRepository implements IActivityRepository {
    
//    async create(dto: any): Promise<any> {
//     //    const doc = await ActivityModel.create({
//     //          workspaceId: dto.workspaceId ? new Types.ObjectId(dto.workspaceId) : undefined,
//     //   projectId: dto.projectId ? new Types.ObjectId(dto.projectId) : undefined,
//     //   taskId: dto.taskId ? new Types.ObjectId(dto.taskId) : undefined,
//     //   performedBy: dto.performedBy ? new Types.ObjectId(dto.performedBy) : undefined,
//     //   affectedUser: dto.affectedUser ? new Types.ObjectId(dto.affectedUser) : undefined,
//     //   type: dto.type,
//     //   message: dto.message,
//     //   metadata: dto.metadata || {},
//     //     })
//     //     return doc
//     // }
    
// }
// }